# Project Context

## Overview

{{ project_description | default("A Python project.") }}

---

## Architecture

High-level description of how the system is structured — major components and how they connect.

```mermaid
flowchart LR
    Input([Input]) --> A[Component A]
    A --> B[Component B]
    B --> C[Component C]
    B --> D[(Storage)]
    C --> Output([Output])
```

### Modules

| Module | Purpose |
|--------|---------|
| `src/{{ package_name }}/cli.py` | Entry point — parses commands and delegates to domain logic |
| `src/{{ package_name }}/settings.py` | Typed configuration loaded from environment |
| `src/{{ package_name }}/logging.py` | Logging setup and logger factory |
| `src/{{ package_name }}/utils/` | Shared helpers with no external dependencies |

---

## Key Design Decisions

- **Decision** — rationale

---

## Non-Goals / Out of Scope

- Item

---

## Project Structure

```
├── src/{{ package_name }}/   # Application source
├── tests/
│   ├── unit/                 # Pure logic, no I/O
│   ├── integration/          # Real filesystem and config
│   └── e2e/                  # Full CLI command execution
├── docs/                     # Documentation
└── scripts/                  # Utility scripts
```
