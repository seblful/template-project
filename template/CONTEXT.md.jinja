# Project Context

## Overview

**What it is**: {{ project_description | default("A Python project.") }}
**Problem it solves**: What pain or need this addresses.

---

## Goals

- Primary goal
- Secondary goal
- What is explicitly out of scope

---

## Architecture

High-level description of how the system is structured — major components and how they connect.

```
[Component A] → [Component B] → [Component C]
         ↑
    [External Service]
```

**Key design decisions:**

- Why X over Y (e.g. "monolith over microservices because...")
- Any non-obvious structural choices

---

## Project Structure

```
/
├── src/{{ package_name }}/   # Application source
├── tests/                    # Test suite (unit/, integration/, e2e/)
├── docs/                     # Documentation
└── scripts/                  # Utility scripts
```

Describe any non-obvious layout choices.

---

## Domain Model

Core concepts and how they relate. Define terms that have specific meaning in this project.

- **Term A**: what it means here
- **Term B**: what it means here