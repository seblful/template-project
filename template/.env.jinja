# Local development settings. Gitignored — safe for real values, never committed.
# Generated once; `copier update` will not overwrite it. See .env.example for every
# supported key.

# Application Settings
APP__APP_NAME={{ project_slug }}

# Logging Settings
LOGGING__FILE_LEVEL=DEBUG
LOGGING__CONSOLE_LEVEL=DEBUG
