# {{ project_slug }}

{{ project_description }}

## Installation

```bash
uv sync
```

## Usage
{% if project_type == "cli" %}
Run the CLI:

```bash
uv run {{ project_slug }} info
```

Or use it as a library:
{% endif %}
```python
from {{ package_name }}.logging import get_logger
from {{ package_name }}.settings import get_settings

log = get_logger(__name__)
log.info("started", environment=get_settings().app.environment)
```

## Configuration

Settings are resolved highest-priority first: real environment variables, then
`.env.local`, then `.env`, then the defaults in `src/{{ package_name }}/settings.py`.
`.env.example` lists every supported key.

See the [reference](reference.md) for the full API.
