"""Top-level package for {{ project_slug }}."""

from importlib.metadata import PackageNotFoundError, version

try:
    __version__ = version("{{ project_slug }}")
except PackageNotFoundError:  # pragma: no cover - a source tree with no install
    __version__ = "0.0.0+unknown"

__author__ = "{{ author_name }}"
__email__ = "{{ author_email }}"

# get_logger/get_settings stay unexported: re-exporting would pull in pydantic and
# structlog for every importer, whether or not they're used.
__all__ = ["__author__", "__email__", "__version__"]
